[pytest]
testpaths = tests
addopts = -q --strict-markers --tb=short
markers =
    postgres: exercita comportamento que só existe em PostgreSQL
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
