# Dependências só de desenvolvimento e CI. Produção usa requirements.txt.
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0

# Geração da monografia e do roteiro de defesa em .docx
# (scripts/gerar_tcc_docx.py, scripts/gerar_defesa_docx.py).
python-docx==1.2.0

# Geração dos slides da defesa (scripts/gerar_defesa_pptx.py).
python-pptx==1.0.2

# Validação FHIR em tests/test_fhir_validacao.py. Os modelos deste pacote são
# gerados da FHIR 4.0.1 — a mesma versão que o sistema declara. Não substitui o
# validador oficial do HL7, que confere também os perfis e exige Java 11.
fhirclient==4.4.0
